# [4. Median of Two Sorted Arrays](https://leetcode.com/problems/median-of-two-sorted-arrays/)
Hard


Given two sorted arrays nums1 and nums2 of size m and n respectively, return the median of the two sorted arrays.

The overall run time complexity should be O(log (m+n)).

 

Example 1:
```
Input: nums1 = [1,3], nums2 = [2]
Output: 2.00000
Explanation: merged array = [1,2,3] and median is 2.
```
Example 2:
```
Input: nums1 = [1,2], nums2 = [3,4]
Output: 2.50000
Explanation: merged array = [1,2,3,4] and median is (2 + 3) / 2 = 2.5.
 ```

Constraints:
- nums1.length == m
- nums2.length == n
- 0 <= m <= 1000
- 0 <= n <= 1000
- 1 <= m + n <= 2000
- -10<sup>6</sup> <= nums1[i], nums2[i] <= 10<sup>6</sup>

## Approach

## Median of Two Sorted Arrays — Quick Revision Notes

### 🔹 Core Intuition
- Median = **partition point**, not a value to search
- We want a split where:
  - Left half has `(m + n + 1) / 2` elements
  - All left elements ≤ all right elements
- Since arrays are sorted, left half is formed by:
  - a prefix of `nums1`
  - a prefix of `nums2`

---

### 🔹 Key Insight
- Fix how many elements to take from `nums1`
- Remaining elements automatically come from `nums2`
- Only **4 boundary values** matter:
  - `L1, R1` from `nums1`
  - `L2, R2` from `nums2`

---

### 🔹 Valid Partition Condition
L1 ≤ R2 AND L2 ≤ R1

yaml
Copy code
This guarantees correct ordering across the split.

---

### 🔹 Why Binary Search Works
- If `L1 > R2` → too many elements from `nums1` → move left
- If `L2 > R1` → too few elements from `nums1` → move right
- Directional feedback ⇒ binary search

---

### 🔹 Median Calculation
- **Odd length** → `max(L1, L2)`
- **Even length** → `(max(L1, L2) + min(R1, R2)) / 2`

---

### 🔹 Approach Summary
1. Always binary search on the **smaller array**
2. Binary search the cut position in `nums1`
3. Derive cut in `nums2` to complete left half
4. Check partition validity using boundary values
5. Return median once valid partition is found

---

### 🔹 Complexity
- Time: `O(log(min(m, n)))`
- Space: `O(1)`

---

### 🔹 One-Line Memory Hook
> “Binary search the cut, not the median.”


## Solution
```java
class Solution {
    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        if(nums1.length > nums2.length)
            return findMedianSortedArrays(nums2, nums1);
        int total = nums1.length + nums2.length;
        int half = (total + 1) / 2;
        int l = 0, r = nums1.length;
        double result = 0d;
        while(l <= r) {
            int i = l + (r - l) / 2;
            int j = half - i;
            int nums1L = i > 0 ? nums1[i-1]:Integer.MIN_VALUE;
            int nums1R = i < nums1.length ? nums1[i]:Integer.MAX_VALUE;
            int nums2L = j > 0 ? nums2[j-1]:Integer.MIN_VALUE;
            int nums2R = j < nums2.length ? nums2[j]:Integer.MAX_VALUE;
            
            if(nums1L <= nums2R && nums2L <= nums1R) {
                if(total % 2 == 0) {
                    return (Math.max(nums1L, nums2L) + Math.min(nums1R, nums2R)) / 2.0;
                } else {
                    return Math.max(nums1L, nums2L);
                }
            } else if(nums1L > nums2R) {
                r = i - 1;
            } else {
                l = i + 1;
            }
        }
        return result;
    }
}
```

## Complexity Analysis
```
- Time Complexity: O(log(m+n))
- Space Complexity: O(1)
```
