# [39. Combination Sum](https://leetcode.com/problems/combination-sum/)
Medium


Given an array of distinct integers candidates and a target integer target, return a list of all unique combinations of candidates where the chosen numbers sum to target. You may return the combinations in any order.

The same number may be chosen from candidates an unlimited number of times. Two combinations are unique if the frequency of at least one of the chosen numbers is different.

It is guaranteed that the number of unique combinations that sum up to target is less than 150 combinations for the given input.

 

Example 1:
```
Input: candidates = [2,3,6,7], target = 7
Output: [[2,2,3],[7]]
Explanation:
2 and 3 are candidates, and 2 + 2 + 3 = 7. Note that 2 can be used multiple times.
7 is a candidate, and 7 = 7.
These are the only two combinations.
```
Example 2:
```
Input: candidates = [2,3,5], target = 8
Output: [[2,2,2,2],[2,3,3],[3,5]]
```
Example 3:
```
Input: candidates = [2], target = 1
Output: []
 ```

Constraints:

- 1 <= candidates.length <= 30
- 1 <= candidates[i] <= 200
- All elements of candidates are distinct.
- 1 <= target <= 500
- 

## 🔑 Core Idea

At each index, we have **2 choices**:

```
1. PICK the element (stay at same index)
2. NOT PICK the element (move to next index)
```

---

## 🧠 Recursion Strategy

We build combinations using backtracking:

- Maintain a `temp` list → current combination
- Maintain `target` → remaining sum

---

## ⚙️ Algorithm

### Base Cases

```java
if (target == 0)
    → valid combination → add to result

if (idx == nums.length || target < 0)
    → stop recursion
```

---

### Recursive Choices

#### 1️⃣ PICK (reuse allowed)

```java
temp.add(nums[idx]);
backtrack(nums, idx, target - nums[idx], res, temp);
temp.remove(temp.size() - 1);
```

- Stay at same index → reuse same element

---

#### 2️⃣ NOT PICK

```java
backtrack(nums, idx + 1, target, res, temp);
```

- Move forward → skip current element

---

## 💻 Java Implementation

```java
class Solution {
    public List<List<Integer>> combinationSum(int[] candidates, int target) {
        List<List<Integer>> res = new ArrayList<>();
        backtrack(candidates, 0, target, res, new ArrayList<>());
        return res;
    }

    private void backtrack(int[] nums, int idx, int target,
                           List<List<Integer>> res, List<Integer> temp){

        if (target == 0) {
            res.add(new ArrayList<>(temp)); // store copy
            return;
        }

        if (idx == nums.length || target < 0) {
            return;
        }

        // PICK (stay at same index)
        temp.add(nums[idx]);
        backtrack(nums, idx, target - nums[idx], res, temp);

        // BACKTRACK
        temp.remove(temp.size() - 1);

        // NOT PICK (move to next index)
        backtrack(nums, idx + 1, target, res, temp);
    }
}
```

---

## 🔄 Backtracking Pattern

```
choose → explore → unchoose
```

Equivalent to:

```
add → recurse → remove
```

---

## 🌳 Recursion Tree (Concept)

```
At each index:

          idx
         /   \
      PICK   NOT PICK
      (stay) (move)
```

---

## 📊 Complexity

### Time Complexity
```
O(2^n * k)
```
- Each element → pick/not pick
- k = average length of combination

### Space Complexity
```
O(k)
```
- recursion stack + temp list

---

## ⚠️ Important Points

### 1️⃣ Why `new ArrayList<>(temp)`?

- `temp` is mutable
- Must store a **copy**, not reference

---

### 2️⃣ Why `remove(size - 1)`?

- Always undo last added element
- Ensures correct backtracking

---

### 3️⃣ Why stay on same index?

```
backtrack(nums, idx, ...)
```

→ allows reuse of same element

---

## 🧠 Pattern Recognition

This is a classic:

- Backtracking
- Pick / Not-Pick recursion
- Subset-style problem

---

## 🗣 Interview One-liner

> “I use pick/not-pick recursion where picking allows reuse by staying at the same index, and I backtrack to explore all combinations.”

---

## 🚀 Related Problems

- Subsets
- Combination Sum II (no reuse)
- Permutations
- Partition problems

---

## 🔥 Key Takeaway

> Backtracking = exploring all choices + undoing decisions to try new paths.
