# [79. Word Search](https://leetcode.com/problems/word-search/)
Medium


Given an m x n grid of characters board and a string word, return true if word exists in the grid.

The word can be constructed from letters of sequentially adjacent cells, where adjacent cells are horizontally or vertically neighboring. The same letter cell may not be used more than once.

 

Example 1:
```
Input: board = [["A","B","C","E"],["S","F","C","S"],["A","D","E","E"]], word = "ABCCED"
Output: true
```
Example 2:
```
Input: board = [["A","B","C","E"],["S","F","C","S"],["A","D","E","E"]], word = "SEE"
Output: true
```
Example 3:
```
Input: board = [["A","B","C","E"],["S","F","C","S"],["A","D","E","E"]], word = "ABCB"
Output: false
``` 

Constraints:

- m == board.length
- n = board[i].length
- 1 <= m, n <= 6
- 1 <= word.length <= 15
- board and word consists of only lowercase and uppercase English letters.

## 🔑 Core Idea

This is a **DFS + Backtracking** problem on a grid.

We:

```
Try starting from every cell
→ Match characters one by one
→ Explore neighbors
→ Backtrack if path fails
```

---

## 🧠 Strategy

1. Loop through each cell in the grid
2. If first character matches → start DFS
3. Recursively explore 4 directions
4. Mark cell as visited during DFS
5. Restore after backtracking

---

## ⚙️ Algorithm

### Step 1: Iterate all cells

```java
for each cell (r, c):
    if dfs(r, c, 0) → return true
```

---

### Step 2: DFS Function

#### Base Case

```java
if (index == word.length)
    return true
```

---

#### Invalid Conditions

```java
if out of bounds OR mismatch:
    return false
```

---

### Step 3: Mark Visited

```java
char temp = board[r][c];
board[r][c] = '#';
```

---

### Step 4: Explore 4 Directions

```java
dfs(r+1, c)
dfs(r-1, c)
dfs(r, c+1)
dfs(r, c-1)
```

---

### Step 5: Backtrack

```java
board[r][c] = temp;
```

---

## 💻 Java Implementation

```java
class Solution {
    public boolean exist(char[][] board, String word) {

        int rows = board.length;
        int cols = board[0].length;

        for (int r = 0; r < rows; r++) {
            for (int c = 0; c < cols; c++) {

                if (dfs(board, word, r, c, 0)) {
                    return true;
                }
            }
        }

        return false;
    }

    private boolean dfs(char[][] board, String word,
                        int r, int c, int index) {

        if (index == word.length()) {
            return true;
        }

        if (r < 0 || c < 0 ||
            r >= board.length || c >= board[0].length ||
            board[r][c] != word.charAt(index)) {
            return false;
        }

        char temp = board[r][c];
        board[r][c] = '#';

        boolean found =
                dfs(board, word, r + 1, c, index + 1) ||
                dfs(board, word, r - 1, c, index + 1) ||
                dfs(board, word, r, c + 1, index + 1) ||
                dfs(board, word, r, c - 1, index + 1);

        board[r][c] = temp;

        return found;
    }
}
```

---

## 🌳 Recursion Flow

```
Start cell → match char
    ↓
Explore 4 directions
    ↓
If path fails → backtrack
```

---

## 📊 Complexity

### Time Complexity
```
O(rows * cols * 4^L)
```

- L = length of word

---

### Space Complexity
```
O(L)
```

- Recursion depth

---

## ⚠️ Important Points

### 1️⃣ Why mark visited?

```
Avoid reusing same cell
```

---

### 2️⃣ Why restore?

```
So other paths can reuse the cell
```

---

### 3️⃣ Why check mismatch early?

```
Prunes invalid paths quickly
```

---

### 4️⃣ Why try all cells?

```
Word can start anywhere
```

---

## 🧠 Pattern Recognition

This is a classic:

- Grid DFS
- Backtracking
- Path search problem

---

## 🗣 Interview One-liner

> “I perform DFS from each cell, marking visited cells temporarily and exploring all 4 directions with backtracking.”

---

## 🚀 Related Problems

- Word Search II (Trie + DFS)
- Number of Islands
- Flood Fill
- Surrounded Regions

---

## 🔥 Key Takeaway

> Grid + DFS + Backtracking = explore all paths while avoiding revisits.
