# 215. Kth Largest Element in an Array

## 🧩 Problem

Given an integer array `nums` and an integer `k`,  
return the **kth largest element** in the array.

⚠️ It is the kth largest in sorted order, not the kth distinct element.

---

## 🔍 Example

Input:
```
nums = [3,2,1,5,6,4], k = 2
```

Sorted descending:
```
6, 5, 4, 3, 2, 1
```

Output:
```
5
```

---

# 🧠 Approach 1: Min Heap (Size k)

## 🔑 Key Insight

We only need the **k largest elements**.

If we maintain a min heap of size `k`:
- The heap always contains the k largest elements seen so far.
- The smallest element in the heap is the kth largest overall.

---

## ⚙️ Algorithm

1. Create a min heap.
2. Iterate through all elements:
   - Insert element into heap.
   - If heap size exceeds `k`, remove the smallest element.
3. Return `heap.peek()`.

---

## 💻 Java Implementation (Heap Approach)

```java
import java.util.PriorityQueue;

class Solution {
    public int findKthLargest(int[] nums, int k) {

        PriorityQueue<Integer> minHeap = new PriorityQueue<>();

        for (int num : nums) {
            minHeap.offer(num);

            if (minHeap.size() > k) {
                minHeap.poll(); // remove smallest
            }
        }

        return minHeap.peek();
    }
}
```

---

## 📊 Complexity Analysis

Time Complexity:
```
O(n log k)
```

Space Complexity:
```
O(k)
```

---

# 🚀 Approach 2: Quickselect (Optimal Average O(n))

## 🔑 Key Insight

We want the element at index:

```
targetIndex = n - k
```

Quickselect:
- Partition like QuickSort.
- Only recurse on one side.
- Average time complexity: `O(n)`

---

## 💻 Java Implementation (Quickselect)

```java
class Solution {
    public int findKthLargest(int[] nums, int k) {
        int target = nums.length - k;
        return quickSelect(nums, 0, nums.length - 1, target);
    }

    private int quickSelect(int[] nums, int left, int right, int target) {
        int pivotIndex = partition(nums, left, right);

        if (pivotIndex == target) {
            return nums[pivotIndex];
        } else if (pivotIndex < target) {
            return quickSelect(nums, pivotIndex + 1, right, target);
        } else {
            return quickSelect(nums, left, pivotIndex - 1, target);
        }
    }

    private int partition(int[] nums, int left, int right) {
        int pivot = nums[right];
        int i = left;

        for (int j = left; j < right; j++) {
            if (nums[j] <= pivot) {
                swap(nums, i, j);
                i++;
            }
        }

        swap(nums, i, right);
        return i;
    }

    private void swap(int[] nums, int i, int j) {
        int temp = nums[i];
        nums[i] = nums[j];
        nums[j] = temp;
    }
}
```

---

## 📊 Quickselect Complexity

Average Time:
```
O(n)
```

Worst Case:
```
O(n^2)
```

Space:
```
O(1)
```

---

# 🎯 When to Use Which?

| Scenario | Best Approach |
|-----------|---------------|
| Simple implementation | Min Heap |
| Streaming input | Min Heap |
| Optimal average performance | Quickselect |
| In-place solution required | Quickselect |

---

# 🗣 Interview One-liners

Heap:
> I maintain a min heap of size k so the smallest element in the heap is the kth largest overall.

Quickselect:
> I use Quickselect to find the element at index n-k in average linear time.
